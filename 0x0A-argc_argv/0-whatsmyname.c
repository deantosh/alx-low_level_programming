#include "main.h"
#include <stdio.h>

/**
 * main - Entry point.
 * @argc: The number of arguments passed.
 * @argv: A pointer to strings passed as arguments.
 *
 * Return: Always (0) Success.
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	(void)argc;
	return (0);
}
